This library implements a class with methods that can color a string either from a predefined palette (loaded from a CSV) or according to specific RGB values.
This implemented from ANSI escape sequences.
